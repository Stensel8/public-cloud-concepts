apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-green
  labels:
    app: public-cloud-concepts
    slot: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: public-cloud-concepts
      slot: green
  template:
    metadata:
      labels:
        app: public-cloud-concepts
        slot: green
    spec:
      containers:
        - name: app
          image: ${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
