# CI Week 3 - Blue-Green deployment naar Google Kubernetes Engine via Artifact Registry.
#
# Automatische deployment per branch:
#   main        -> bouwt en deployed slot "blue"  (productie)
#   development -> bouwt en deployed slot "green" (test)
#
# BELANGRIJK: Beide deployments draaien tegelijkertijd in het cluster!
# De development branch kan andere code bevatten dan main - ze hoeven niet
# gesynchroniseerd te zijn. Zo kun je nieuwe features testen op green voordat
# je ze merged naar main of live zet.
#
# Stappen:
#   1. Bepaal de slot op basis van de branch.
#   2. Authenticeer met GCP via het github-pipeline service account.
#   3. Controleer of het GKE-cluster beschikbaar is.
#   4. Configureer Docker voor Artifact Registry en haal GKE-credentials op.
#   5. Bouw het image en push het naar Artifact Registry met de juiste slot-tag.
#   6. Deploy naar GKE via envsubst + kubectl apply, forceer een rollout restart
#      en wacht op een succesvolle rollout (timeout 120s).
#
# Let op: de Kubernetes Service wordt hier NIET aangepast. De selector (blue/green)
# wordt beheerd via de switch-slot.yml workflow. Eenmalig aanmaken via kubectl apply.
#
# Secrets benodigd: GCP_PROJECT_ID, GCP_SA_KEY, GKE_CLUSTER, GKE_ZONE
name: CI Week 3 - Blue-Green Deploy

on:
  push:
    branches: [main, development]
    paths:
      - '.github/workflows/ci_week3.yml'
      - 'static/docs/week-3/**'
      - 'static/docs/week-1/bestanden/**'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GKE_CLUSTER: ${{ secrets.GKE_CLUSTER }}
  GKE_ZONE: ${{ secrets.GKE_ZONE }}
  GAR_ZONE: europe-west4
  GAR_REPO: public-cloud-concepts
  IMAGE: app

jobs:
  build-and-deploy:
    name: Build, push & deploy
    runs-on: ubuntu-latest

    steps:
      # Haal de broncode op uit de repository.
      - name: Haal broncode op
        uses: actions/checkout@v6

      # Bepaal op basis van de branch welke slot gebruikt wordt:
      #   main        -> blue  (productie)
      #   development -> green (test)
      - name: Bepaal deployment slot
        id: slot
        run: |
          if [ "${{ github.ref_name }}" == "main" ]; then
            echo "slot=blue" >> $GITHUB_OUTPUT
          else
            echo "slot=green" >> $GITHUB_OUTPUT
          fi

      # Authenticeer bij Google Cloud met het service account.
      - name: Authenticeer bij GCP
        uses: google-github-actions/auth@v3
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Controleer of het GKE-cluster beschikbaar is.
      # Als het cluster niet bestaat of niet bereikbaar is, wordt de workflow geannuleerd.
      - name: Controleer beschikbaarheid GKE-cluster
        id: cluster_check
        run: |
          if gcloud container clusters describe "${{ env.GKE_CLUSTER }}" \
            --location "${{ env.GKE_ZONE }}" \
            --project "${{ env.PROJECT_ID }}" >/dev/null 2>&1; then
            echo "available=true" >> "$GITHUB_OUTPUT"
            echo "Week 3 cluster gevonden; deployment wordt uitgevoerd." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Week 3 omgeving mogelijk niet beschikbaar. Dit kan normaal zijn (studentquota, 1 cluster tegelijk), of tijdelijk door GKE/Google Cloud/GitHub API bereikbaarheid."
            {
              echo "### Week 3 deployment overgeslagen"
              echo "Week 3 omgeving mogelijk niet beschikbaar."
              echo "Dit kan normaal zijn door studentquota (vaak maar 1 cluster tegelijk), of tijdelijk door GKE/Google Cloud/GitHub API bereikbaarheid."
              echo "De workflow is daarom bewust overgeslagen in plaats van hard gefaald."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      # Als het cluster niet beschikbaar is: annuleer de workflow via de GitHub API.
      # continue-on-error: true zorgt dat de annulering zelf geen extra fout geeft.
      - name: Annuleer workflow als omgeving niet beschikbaar is
        if: steps.cluster_check.outputs.available != 'true'
        continue-on-error: true
        uses: actions/github-script@v9
        with:
          script: |
            await github.rest.actions.cancelWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: context.runId
            })

      # Configureer Docker zodat het kan pushen naar Google Artifact Registry.
      - name: Configureer Docker voor Artifact Registry
        if: steps.cluster_check.outputs.available == 'true'
        run: |
          gcloud auth print-access-token | docker login -u oauth2accesstoken \
            --password-stdin https://${{ env.GAR_ZONE }}-docker.pkg.dev

      # Haal de kubectl-configuratie op zodat we commando's naar het cluster kunnen sturen.
      - name: Haal GKE-credentials op
        if: steps.cluster_check.outputs.available == 'true'
        uses: google-github-actions/get-gke-credentials@v3
        with:
          cluster_name: ${{ env.GKE_CLUSTER }}
          location: ${{ env.GKE_ZONE }}

      # Bouw het Docker image met de juiste slot-tag (blue of green).
      - name: Bouw Docker image
        if: steps.cluster_check.outputs.available == 'true'
        run: |
          SLOT="${{ steps.slot.outputs.slot }}"
          docker build \
            --tag "${{ env.GAR_ZONE }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPO }}/${{ env.IMAGE }}:${SLOT}" \
            "./static/docs/week-1/bestanden/"

      # Push het gebouwde image naar Artifact Registry, zodat GKE het kan ophalen.
      - name: Push naar Artifact Registry
        if: steps.cluster_check.outputs.available == 'true'
        run: |
          docker push "${{ env.GAR_ZONE }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPO }}/${{ env.IMAGE }}:${{ steps.slot.outputs.slot }}"

      # Vervang ${IMAGE_TAG} in het deployment-manifest en stuur het naar kubectl.
      # envsubst leest van stdin, zodat er geen tijdelijk bestand nodig is.
      - name: Pas deployment-manifest toe
        if: steps.cluster_check.outputs.available == 'true'
        run: |
          SLOT="${{ steps.slot.outputs.slot }}"
          export IMAGE_TAG="${{ env.GAR_ZONE }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPO }}/${{ env.IMAGE }}:${SLOT}"
          envsubst < "static/docs/week-3/bestanden/deployment-${SLOT}.yml" | kubectl apply -f -

      # Forceer een herstart zodat Kubernetes het nieuw gepushte image ophaalt.
      # Zonder dit herkent Kubernetes geen wijziging als de tag (bijv. :green) gelijk blijft.
      - name: Start rollout opnieuw
        if: steps.cluster_check.outputs.available == 'true'
        run: kubectl rollout restart deployment/deployment-${{ steps.slot.outputs.slot }}

      # Wacht op een succesvolle rollout (timeout 120s).
      # Bij mislukken worden pod-details getoond om debuggen te vergemakkelijken.
      - name: Wacht op rollout
        if: steps.cluster_check.outputs.available == 'true'
        run: |
          SLOT="${{ steps.slot.outputs.slot }}"
          if ! kubectl rollout status deployment/deployment-${SLOT} --timeout=120s; then
            echo "--- Rollout mislukt. Pod-status: ---"
            kubectl get pods -l slot=${SLOT} -o wide
            echo "--- Pod beschrijving: ---"
            kubectl describe pods -l slot=${SLOT}
            exit 1
          fi
          kubectl get pods -l slot=${SLOT}
