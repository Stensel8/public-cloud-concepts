apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "public-cloud-concepts.fullname" . }}-test-connection"
  labels:
    {{- include "public-cloud-concepts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "public-cloud-concepts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
