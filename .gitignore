# Could contain sensitive information, so we ignore it
*.pdf
*.docx
/.claude

# Hugo build output
/public/

# Hugo build lock file
.hugo_build.lock

# Hugo lokale build-cache (wordt automatisch opnieuw gegenereerd)
/resources/
*.zip
